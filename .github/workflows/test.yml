name: Tests

on: [push]

jobs:
  python-tests:
    runs-on: ubuntu-latest
    name: Python ${{ matrix.python-version }} - Django ${{ matrix.django-version }}${{ matrix.valkey && ' (valkey)' || '' }}

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13", "3.14"]
        django-version: ["4.2", "5.2", "6.0"]
        valkey: [false, true]
        exclude:
          # -------------------------------------------------
          # django 6.0 requires python >=3.12
          # -------------------------------------------------
          - python-version: "3.9"
            django-version: "6.0"
          - python-version: "3.10"
            django-version: "6.0"
          - python-version: "3.11"
            django-version: "6.0"
          # -------------------------------------------------
          # django 4.2 upper python constraint (adjust if needed)
          # -------------------------------------------------
          - python-version: "3.14"
            django-version: "4.2"
          # -------------------------------------------------
          # valkey requires python >=3.10
          # -------------------------------------------------
          - python-version: "3.9"
            valkey: true
    services:
      redis:
        image: redis:7.4.1
        ports: ["6379:6379"]
      valkey:
        image: valkey/valkey:9.0.0
        ports: ["6380:6379"]
      memcached:
        image: memcached:1.6
        ports: ["11211:11211"]
      postgres:
        image: postgres:16
        ports: ["5432:5432"]
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package and dependencies
        run: |
          if [ "${{ matrix.valkey }}" = "true" ]; then
            pip install .[dev,valkey]
          else
            pip install .[dev]
          fi
          pip install "Django~=${{ matrix.django-version }}.0"

      - name: Run tests with coverage
        run: pytest --cov=dj_cache_panel --cov-report=xml
        env:
          TEST_DB_BACKEND: postgresql
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: >
            unittests,
            python-${{ matrix.python-version }},
            django-${{ matrix.django-version }}
            ${{ matrix.valkey && ',valkey' || '' }}
          fail_ci_if_error: true